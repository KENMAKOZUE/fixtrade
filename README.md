# fixtrade

Ѕазовый проект на React + TypeScript + SWC с Vite.

##  оманды

- `npm install` Ч установить зависимости
- `npm run dev` Ч запустить проект в режиме разработки
- `npm run build` Ч собрать проект
- `npm run preview` Ч запустить превью сборки

# fixtrade
