# Copilot Project Instructions

- Project: React + TypeScript + SWC starter
- Root: current workspace
- Ensure `README.md` exists and is up to date
- Ensure package scripts are defined for `dev`, `build`, and `preview`
